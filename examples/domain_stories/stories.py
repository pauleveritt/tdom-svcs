"""Catalog for tdom-svcs domain witness stories."""

from storyville import Catalog


def this_catalog() -> Catalog:
    """Return the tdom-svcs domain stories catalog."""
    return Catalog(title="tdom-svcs Domain Stories")
