"""Component evidence section for tdom-svcs domain stories."""

from storyville import Section


def this_section() -> Section:
    """Return the component evidence section."""
    return Section(title="Component Evidence")
